<!--NAVIGATION BAR-->
<nav class="navbar navbar-light" style="background-color: #7D82CD">  
  <form class="form-inline">
     <ul class="navbar-nav mr-auto">
      </ul>
      <div class="btn btn-primary">
        &nbsp;
        <i class="fa fa-user-plus fa-lg" aria-hidden="true"></i>
        &nbsp;
        <%= link_to 'Add a new student', new_student_path(@student), {:style=>'color:#FFFFFF;' 'text-decoration:none;' } %>
        &nbsp;
      </div>
  </form>
</nav>

<!--SEARCH FORM-->
<div class ="container">
  <div class="col-6">
  <%= form_tag(students_path, method: "get",id: "search-box") do %>
    <%= label_tag("Search by students name:") %>
    <br />
    <%= text_field_tag(:search, params[:search], class: 'search-bar') %>
    <br />
    <br />
    <%= submit_tag("Search", name: nil) %>
  <% end %>
  
 
 <!--ALERT MESSAGE FOR INVALID SEARCH -->
  <% if @student.blank? %>
    <h3>No students found with the name(s) "<%= params[:search]%>"</h3>
    <% end %>
  </div>


<!--LIST OF CURENT STUDENTS IN DB  -->
<div class="col-6">
<h4>Students</h4>
<table class="table">
  <tr>
    <th>Name</th>
    <th>Grade level</th>
  </tr>
 
<% @student.each do |student| %>
  <tr>
    <td><%= student.name %></td>
    <td><%= student.grade %></td>
    <td><%= link_to "Show", student %></td>
    <td><%= link_to "Edit", edit_student_path(student) %></td>
    <td><%= link_to "Remove", student, method: :delete, data: { confirm: "Are you sure?" } %></td>
  </tr>
<% end %>
</table>
</div>

</div> <!--END OF CONTAINER-->

<!--JAVASCRIPT TO CLEAR SEARCH INPUT FIELD AFTER PAGE RELOADS -->
<script>
  $('.search-bar').val("");
</script>